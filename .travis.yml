language: php
php:
  - 7.3
  - 7.4
  - 8.0
  - 8.1
cache:
  directories:
    - $HOME/.composer/cache
before_install:
  - composer self-update --1
  - composer validate
install:
  - composer install
script:
  - vendor/bin/phpunit tests
  - ./build.sh
