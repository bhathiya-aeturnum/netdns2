language: php
php:
  - 5.6
  - 7.1
  - 7.2
  - 7.3
cache:
  directories:
    - $HOME/.composer/cache
before_install:
  - composer self-update --1
  - composer validate
install:
  - composer install
script:
  - vendor/bin/phpunit tests
  - ./build.sh
